import React from "react";
import {Link} from "react-router-dom";
const Login = () => {
    return (
        <div>
            <h2>Login Page</h2>
            <Link to="/">Go to Home</Link>
        </div>
    );
};

export default Login;